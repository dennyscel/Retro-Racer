// ES module nativo — bindings compartilhados explícitos via globalThis durante a migração arquitetural.
globalThis.drawTree = function drawTree(x,y,s){ ctx.fillStyle='#5a3a1a'; ctx.fillRect(x-2*s,y-20*s,4*s,20*s); ctx.fillStyle='#0a5518'; ctx.beginPath(); ctx.arc(x,y-30*s,15*s,0,Math.PI*2); ctx.fill(); ctx.fillStyle='#0e701d'; ctx.beginPath(); ctx.arc(x-9*s,y-26*s,11*s,0,Math.PI*2); ctx.arc(x+9*s,y-26*s,11*s,0,Math.PI*2); ctx.fill(); }
globalThis.drawPine = function drawPine(x,y,s){ ctx.fillStyle='#4a2a10'; ctx.fillRect(x-2*s,y-18*s,4*s,18*s); ctx.fillStyle='#064d18'; ctx.beginPath(); ctx.moveTo(x,y-54*s); ctx.lineTo(x-18*s,y-20*s); ctx.lineTo(x+18*s,y-20*s); ctx.closePath(); ctx.fill(); ctx.fillStyle='#0a6b20'; ctx.beginPath(); ctx.moveTo(x,y-42*s); ctx.lineTo(x-22*s,y-6*s); ctx.lineTo(x+22*s,y-6*s); ctx.closePath(); ctx.fill(); }
globalThis.drawCactus = function drawCactus(x,y,s){ ctx.fillStyle='#2d6b1e'; ctx.fillRect(x-3*s,y-28*s,6*s,28*s); ctx.fillRect(x+4*s,y-20*s,3*s,12*s); ctx.fillRect(x-7*s,y-16*s,3*s,10*s); }
globalThis.drawMarker = function drawMarker(x,y,s){ ctx.fillStyle='#f8f8f8'; ctx.fillRect(x-2*s,y-24*s,4*s,24*s); ctx.fillStyle='#d31818'; ctx.fillRect(x-2*s,y-18*s,4*s,6*s); ctx.fillRect(x-2*s,y-6*s,4*s,6*s); }
globalThis.drawFlag = function drawFlag(x,y,s){ ctx.fillStyle='#e8e8e8'; ctx.fillRect(x-1.5*s,y-42*s,3*s,42*s); ctx.fillStyle='#ffcc00'; ctx.fillRect(x+1.5*s,y-42*s,20*s,11*s); ctx.fillStyle='#111'; ctx.fillRect(x+5*s,y-39*s,4*s,4*s); ctx.fillRect(x+14*s,y-35*s,4*s,4*s); }
globalThis.drawChevron = function drawChevron(x,y,s,dir){ ctx.fillStyle='#333'; ctx.fillRect(x-1.4*s,y-27*s,2.8*s,27*s); ctx.fillStyle='#ffcc00'; ctx.fillRect(x-15*s,y-43*s,30*s,17*s); ctx.fillStyle='#111'; ctx.beginPath(); if(dir>0){ctx.moveTo(x-8*s,y-40*s);ctx.lineTo(x+7*s,y-34.5*s);ctx.lineTo(x-8*s,y-29*s);ctx.lineTo(x-3*s,y-34.5*s);} else {ctx.moveTo(x+8*s,y-40*s);ctx.lineTo(x-7*s,y-34.5*s);ctx.lineTo(x+8*s,y-29*s);ctx.lineTo(x+3*s,y-34.5*s);} ctx.closePath();ctx.fill(); }
globalThis.drawBillboard = function drawBillboard(x,y,s){ ctx.fillStyle='#333'; ctx.fillRect(x-22*s,y-4*s,3.5*s,32*s); ctx.fillRect(x+18*s,y-4*s,3.5*s,32*s); ctx.fillStyle='#111'; ctx.fillRect(x-30*s,y-56*s,60*s,32*s); ctx.fillStyle='#ffcc00'; ctx.fillRect(x-27*s,y-53*s,54*s,26*s); ctx.fillStyle='#111'; ctx.font=`900 ${Math.max(4,6.4*s)}px 'Courier New'`; ctx.textAlign='center'; const nm=activeCircuit?String(activeCircuit.name||'RACE').toUpperCase():'RACE'; ctx.fillText(nm.slice(0,12),x,y-40*s); ctx.fillText('CLÁSSICOS',x,y-31*s); ctx.textAlign='left'; }
globalThis.drawLampPost = function drawLampPost(x,y,s){ ctx.fillStyle='#555'; ctx.fillRect(x-2*s,y-58*s,4*s,58*s); ctx.fillStyle='#ffe066'; ctx.fillRect(x-8*s,y-62*s,16*s,6*s); ctx.fillStyle='#fff'; ctx.fillRect(x-5*s,y-60*s,10*s,3*s); }
globalThis.drawSignPost = function drawSignPost(x,y,s){ ctx.fillStyle='#777'; ctx.fillRect(x-2*s,y-35*s,4*s,35*s); ctx.fillStyle='#ff4444'; ctx.fillRect(x-14*s,y-44*s,28*s,10*s); ctx.fillStyle='#fff'; ctx.font=`${Math.max(4,6*s)}px 'Courier New'`; ctx.textAlign='center'; ctx.fillText('←',x-4*s,y-36*s); ctx.fillText('→',x+4*s,y-36*s); ctx.textAlign='left'; }
globalThis.drawPitDistance = function drawPitDistance(x,y,s,label){ ctx.fillStyle='#333';ctx.fillRect(x-2*s,y-48*s,4*s,48*s);ctx.fillStyle='#111';ctx.fillRect(x-25*s,y-64*s,50*s,20*s);ctx.strokeStyle='#ffcc00';ctx.lineWidth=Math.max(1,2*s);ctx.strokeRect(x-25*s,y-64*s,50*s,20*s);ctx.fillStyle='#ffcc00';ctx.font=`900 ${Math.max(5,7*s)}px 'Courier New'`;ctx.textAlign='center';ctx.fillText(label,x,y-50*s);ctx.textAlign='left';}
globalThis.drawPitSign = function drawPitSign(x,y,s){ ctx.fillStyle='#333'; ctx.fillRect(x-2*s,y-48*s,4*s,48*s); ctx.fillStyle='#ffcc00'; ctx.fillRect(x-22*s,y-62*s,44*s,18*s); ctx.fillStyle='#111'; ctx.font=`900 ${Math.max(6,9*s)}px 'Courier New'`; ctx.textAlign='center'; ctx.fillText('PIT',x,y-49*s); ctx.textAlign='left'; }
globalThis.drawPitCrew = function drawPitCrew(x,y,s){ ctx.fillStyle='#222'; ctx.fillRect(x-12*s,y-24*s,24*s,24*s); ctx.fillStyle='#ffcc00'; ctx.fillRect(x-9*s,y-38*s,18*s,14*s); ctx.fillStyle='#111'; ctx.fillRect(x-6*s,y-35*s,12*s,4*s); }
globalThis.drawDecoration = function drawDecoration(seg, dec, horizon) {
    if (!seg.p1.camera || seg.p1.camera.z <= renderCameraDepth) return;
    const p = seg.p1.screen; if (p.w < 12) return; const x = p.x + p.w * dec.offset; const y = p.y; if (y < horizon + 6 || y > height + 80) return; const scale = MathUtils.limit(p.w / 390, .055, 1.45); if (scale < .075) return; const alpha = MathUtils.limit((p.w - 10) / 90, .2, 1); ctx.globalAlpha = alpha;
    switch (dec.type) { case 'tree': drawTree(x,y,scale); break; case 'pine': drawPine(x,y,scale); break; case 'cactus': drawCactus(x,y,scale); break; case 'marker': drawMarker(x,y,scale); break; case 'flag': drawFlag(x,y,scale); break; case 'chevronRight': drawChevron(x,y,scale,1); break; case 'chevronLeft': drawChevron(x,y,scale,-1); break; case 'billboard': drawBillboard(x,y,scale); break; case 'lampPost': drawLampPost(x,y,scale); break; case 'signPost': drawSignPost(x,y,scale); break; case 'pit200': drawPitDistance(x,y,scale,'PIT 200m'); break; case 'pit100': drawPitDistance(x,y,scale,'PIT 100m'); break; case 'pitSign': drawPitSign(x,y,scale); break; case 'pitCrew': drawPitCrew(x,y,scale); break; }
    ctx.globalAlpha = 1;
}

window.RRCore.runtime.bind('CAR_MODELS',{ hero:{ h:.66, roof:.58, body:1, spoiler:true, exhaust:true, stripe:true, wideTail:true }, sport:{ h:.58, roof:.52, body:.96, spoiler:true, exhaust:false, stripe:true, wideTail:false }, coupe:{ h:.55, roof:.48, body:.9, spoiler:false, exhaust:false, stripe:true, wideTail:false }, rally:{ h:.62, roof:.55, body:1.02, spoiler:true, exhaust:false, stripe:true, wideTail:true }, muscle:{ h:.57, roof:.44, body:1.08, spoiler:false, exhaust:false, stripe:false, wideTail:true } });
globalThis.carClamp = function carClamp(v,min,max){ return Math.max(min, Math.min(max, v)); }
globalThis.carShade = function carShade(hex, amount){ if(typeof hex !== 'string' || hex[0] !== '#' || hex.length < 7) return hex; const n = parseInt(hex.slice(1),16); if(!Number.isFinite(n)) return hex; const r=carClamp((n>>16)+amount,0,255), g=carClamp(((n>>8)&255)+amount,0,255), b=carClamp((n&255)+amount,0,255); return `rgb(${r},${g},${b})`; }
globalThis.carPoly = function carPoly(points, fill, context = ctx){ context.fillStyle = fill; context.beginPath(); context.moveTo(points[0][0],points[0][1]); for(let i=1;i<points.length;i++) context.lineTo(points[i][0],points[i][1]); context.closePath(); context.fill(); }
globalThis.carStroke = function carStroke(points, color, lineWidth, context = ctx){ context.strokeStyle = color; context.lineWidth = lineWidth; context.beginPath(); context.moveTo(points[0][0],points[0][1]); for(let i=1;i<points.length;i++) context.lineTo(points[i][0],points[i][1]); context.closePath(); context.stroke(); }
globalThis.carRoundRect = function carRoundRect(x,y,w,h,r,fill, context = ctx){ r=Math.min(r,Math.abs(w)/2,Math.abs(h)/2); context.fillStyle=fill; context.beginPath(); context.moveTo(x+r,y); context.lineTo(x+w-r,y); context.quadraticCurveTo(x+w,y,x+w,y+r); context.lineTo(x+w,y+h-r); context.quadraticCurveTo(x+w,y+h,x+w-r,y+h); context.lineTo(x+r,y+h); context.quadraticCurveTo(x,y+h,x,y+h-r); context.lineTo(x,y+r); context.quadraticCurveTo(x,y,x+r,y); context.closePath(); context.fill(); }
globalThis.drawSimpleCar = function drawSimpleCar(x,y,w,color,steer,braking, context = ctx){ const h=Math.max(5,w*.52), lean=MathUtils.limit(steer,-1,1)*w*.05; context.save(); context.translate(Math.floor(x),Math.floor(y)); context.fillStyle='rgba(0,0,0,.38)'; context.fillRect(-w*.43,-h*.12,w*.86,h*.12); context.fillStyle='#080808'; context.fillRect(-w*.46+lean,-h*.70,w*.16,h*.45); context.fillRect(w*.30+lean,-h*.70,w*.16,h*.45); context.fillStyle=color; context.fillRect(-w*.40+lean,-h*.78,w*.80,h*.55); context.fillStyle='#111'; context.fillRect(-w*.22+lean,-h*.92,w*.44,h*.22); context.fillStyle=braking?'#fff2dc':'#ff2222'; context.fillRect(-w*.32,-h*.38,w*.16,h*.08); context.fillRect(w*.16,-h*.38,w*.16,h*.08); context.restore(); }

globalThis.drawCarLivery=function drawCarLivery(context,w,h,top,lean,v){if(!v||!Array.isArray(v.layers))return;context.save();context.beginPath();context.moveTo(-w*.41+lean,top+h*.31);context.lineTo(w*.41+lean,top+h*.31);context.lineTo(w*.53-lean,top+h*.66);context.lineTo(w*.43-lean,-h*.04);context.lineTo(-w*.43-lean,-h*.04);context.lineTo(-w*.53-lean,top+h*.66);context.closePath();context.clip();const y0=top+h*.36,y1=top+h*.91;for(const l of v.layers){const p=Number(l&&l.pattern);if(p<0)continue;context.globalAlpha=.84;context.fillStyle=l.color||'#fff';context.strokeStyle=l.color||'#fff';context.lineWidth=Math.max(1,w*.03);if(p===0)context.fillRect(-w*.055,y0,w*.11,y1-y0);else if(p===1){context.fillRect(-w*.14,y0,w*.07,y1-y0);context.fillRect(w*.07,y0,w*.07,y1-y0);}else if(p===2){context.fillRect(-w*.48,y0,w*.10,y1-y0);context.fillRect(w*.38,y0,w*.10,y1-y0);}else if(p===3){context.save();context.rotate(-.42);context.fillRect(-w*.1,top+h*.20,w*.2,h*.95);context.restore();}else if(p===4){for(let yy=y0;yy<y1;yy+=h*.18){context.beginPath();context.moveTo(-w*.34,yy);context.lineTo(0,yy+h*.1);context.lineTo(w*.34,yy);context.lineTo(w*.34,yy+h*.05);context.lineTo(0,yy+h*.15);context.lineTo(-w*.34,yy+h*.05);context.closePath();context.fill();}}else if(p===5){for(let i=-2;i<=2;i++){context.beginPath();context.moveTo(i*w*.16,top+h*.9);context.lineTo(i*w*.16-w*.08,top+h*.55);context.lineTo(i*w*.16+w*.08,top+h*.72);context.closePath();context.fill();}}else if(p===6){for(let r=0;r<4;r++)for(let c=0;c<5;c++)if((r+c)%2===0)context.fillRect(-w*.45+c*w*.18,y0+r*h*.13,w*.18,h*.13);}else if(p===7){const g=context.createLinearGradient(-w*.45,0,w*.45,0);g.addColorStop(0,l.color||'#fff');g.addColorStop(1,'rgba(255,255,255,0)');context.fillStyle=g;context.fillRect(-w*.48,y0,w*.96,y1-y0);}else if(p===8){for(let yy=y0;yy<y1;yy+=h*.15)context.fillRect(-w*.42+(Math.floor((yy-y0)/(h*.15))%2)*w*.14,yy,w*.28,h*.075);}else if(p===9){context.beginPath();context.moveTo(-w*.08,y0);context.lineTo(w*.14,top+h*.52);context.lineTo(w*.02,top+h*.52);context.lineTo(w*.17,y1);context.lineTo(-w*.15,top+h*.64);context.lineTo(-w*.03,top+h*.64);context.closePath();context.fill();}else if(p===10)context.fillRect(0,y0,w*.5,y1-y0);else if(p===11){for(let yy=y0;yy<y1;yy+=h*.18){context.beginPath();for(let x=-w*.42;x<=w*.42;x+=w*.07)context.lineTo(x,yy+Math.sin(x/w*18)*h*.035);context.stroke();}}}context.restore();if(w>=42){context.save();context.textAlign='center';context.strokeStyle='#000';context.lineWidth=Math.max(1,w*.014);context.font=`900 ${Math.max(6,Math.floor(w*.1))}px 'Courier New'`;const n=String(v.number??0).padStart(2,'0');context.strokeText(n,0,top+h*.56);context.fillStyle='#fff';context.fillText(n,0,top+h*.56);if(w>=72&&globalThis.paintShopSponsorName){context.font=`900 ${Math.max(5,Math.floor(w*.052))}px 'Courier New'`;const sp=paintShopSponsorName(v.sponsor).slice(0,10);context.strokeText(sp,0,top+h*.68);context.fillStyle='#ffcc00';context.fillText(sp,0,top+h*.68);}context.restore();}}

globalThis.drawCarGeneric = function drawCarGeneric(context, x, y, cw, color, isPlayer, steer, carData = null, forceLarge = false) {
    const modelName = isPlayer ? 'hero' : (carData && carData.model ? carData.model : 'sport'); const model = CAR_MODELS[modelName] || CAR_MODELS.sport;
    const localWidth = context.canvas ? context.canvas.width : width;
    const maxW = forceLarge ? cw : isPlayer ? Math.max(76, Math.min(110, width * .175)) : Math.max(48, Math.min(78, width * .145));
    const w = Math.floor(MathUtils.limit(cw, 8, maxW)); const h = Math.floor(w * model.h); if (w < 7 || h < 6) return;
    const braking = isPlayer ? (keys.ArrowDown || touchDrive.brake) : !!(carData && carData.braking);
    if (!isPlayer && w < 16 && !forceLarge) { drawSimpleCar(x,y,w,color,steer,braking,context); return; }
    const speedRatio = MathUtils.limit(speed / maxSpeed, 0, 1); const playerNitroActive = isPlayer && nitroWasActive && nitro > .02 && speedRatio > .16; const opponentNitro = !isPlayer && carData && carData.nitroBurst > 0; const accelerating = (isPlayer && (keys.ArrowUp || touchDrive.active) && speedRatio > .18) || opponentNitro;
    const livery=(carData&&carData.livery)||(isPlayer&&globalThis.carLivery?carLivery(currentCar().id):null); const body=color, light=carShade(color,42), mid=carShade(color,8), dark=carShade(color,-46), deep=carShade(color,-78); const rot=MathUtils.limit(steer,-1.15,1.15)*(isPlayer?.085:.055), lean=MathUtils.limit(steer,-1,1)*w*(isPlayer?.045:.03); const stripe=isPlayer?'#111111':(carData&&carData.stripe?carData.stripe:'#ffffff'); const px=Math.floor(x), py=Math.floor(y);
    context.save(); context.globalAlpha=isPlayer?.55:.42; context.fillStyle='#000'; context.beginPath(); context.ellipse(px,py-h*.035,w*.60,h*.205,0,0,Math.PI*2); context.fill(); context.globalAlpha=1; context.restore();
    context.save(); context.translate(px,py); context.rotate(rot); if(isPlayer) context.scale(1.02,1); else context.scale(1,.96); const top=-h, bottom=0; if(gameMode==='race'&&(currentWeather()==='rain'||currentWeather()==='storm')&&speedRatio>.34){context.save();context.globalAlpha=.13+speedRatio*.13;context.fillStyle='#dff6ff';context.beginPath();context.moveTo(-w*.38,-h*.02);context.lineTo(-w*.62,h*.18);context.lineTo(-w*.12,h*.04);context.closePath();context.fill();context.beginPath();context.moveTo(w*.38,-h*.02);context.lineTo(w*.62,h*.18);context.lineTo(w*.12,h*.04);context.closePath();context.fill();context.restore();}
    const rearWheelW=w*.19, rearWheelH=h*.45, frontWheelW=w*.15, frontWheelH=h*.34;
    carRoundRect(-w*.54+lean,top+h*.46,rearWheelW,rearWheelH,w*.035,'#060606',context); carRoundRect(w*.35+lean,top+h*.46,rearWheelW,rearWheelH,w*.035,'#060606',context);
    carRoundRect(-w*.52+lean,top+h*.24,frontWheelW,frontWheelH,w*.03,'#080808',context); carRoundRect(w*.37+lean,top+h*.24,frontWheelW,frontWheelH,w*.03,'#080808',context);
    context.fillStyle='#202020';
    context.fillRect(-w*.515+lean,top+h*.54,w*.135,h*.11); context.fillRect(w*.380+lean,top+h*.54,w*.135,h*.11);
    context.fillRect(-w*.495+lean,top+h*.305,w*.105,h*.08); context.fillRect(w*.390+lean,top+h*.305,w*.105,h*.08);
    context.fillStyle='#3a3a3a';
    context.fillRect(-w*.485+lean,top+h*.345,w*.085,h*.022); context.fillRect(w*.400+lean,top+h*.345,w*.085,h*.022);
    if(model.spoiler){ carRoundRect(-w*.45+lean,top+h*.12,w*.90,h*.075,w*.025,deep,context); context.fillStyle='#101010'; context.fillRect(-w*.34+lean,top+h*.185,w*.052,h*.14); context.fillRect(w*.288+lean,top+h*.185,w*.052,h*.14); context.fillStyle=light; context.globalAlpha=.42; context.fillRect(-w*.38+lean,top+h*.135,w*.76,h*.018); context.globalAlpha=1; }
    carPoly([[-w*.48+lean,top+h*.58],[w*.48+lean,top+h*.58],[w*.43-lean,bottom-h*.02],[-w*.43-lean,bottom-h*.02]],deep,context);
    carPoly([[-w*.41+lean,top+h*.31],[w*.41+lean,top+h*.31],[w*.53-lean,top+h*.66],[w*.43-lean,bottom-h*.04],[-w*.43-lean,bottom-h*.04],[-w*.53-lean,top+h*.66]],body,context);
    carPoly([[-w*.41+lean,top+h*.35],[-w*.53-lean,top+h*.66],[-w*.43-lean,bottom-h*.04],[-w*.20-lean,top+h*.67]],dark,context); carPoly([[w*.41+lean,top+h*.35],[w*.53-lean,top+h*.66],[w*.43-lean,bottom-h*.04],[w*.20-lean,top+h*.67]],dark,context);
    carPoly([[-w*.30+lean,top+h*.42],[w*.30+lean,top+h*.42],[w*.24-lean,top+h*.64],[-w*.24-lean,top+h*.64]],mid,context); carPoly([[-w*.27+lean,top+h*.07],[w*.27+lean,top+h*.07],[w*.36+lean,top+h*.36],[-w*.36+lean,top+h*.36]],'#080808',context);
    const glass = context.createLinearGradient(0,top+h*.08,0,top+h*.38); glass.addColorStop(0,'#d7fbff'); glass.addColorStop(.35,'#49b5e8'); glass.addColorStop(.72,'#123f63'); glass.addColorStop(1,'#071622'); carPoly([[-w*.215+lean,top+h*.115],[w*.215+lean,top+h*.115],[w*.295+lean,top+h*.335],[-w*.295+lean,top+h*.335]],glass,context);
    context.globalAlpha=.46; carPoly([[-w*.175+lean,top+h*.135],[-w*.025+lean,top+h*.135],[-w*.145+lean,top+h*.315],[-w*.245+lean,top+h*.315]],'#fff',context); context.globalAlpha=1;
    if(livery)drawCarLivery(context,w,h,top,lean,livery);
    if(model.stripe&&!livery){ context.globalAlpha=isPlayer?.95:.88; carPoly([[-w*.045+lean*.18,top+h*.36],[w*.045+lean*.18,top+h*.36],[w*.030-lean*.18,top+h*.82],[-w*.030-lean*.18,top+h*.82]],stripe,context); context.globalAlpha=1; }
    context.globalAlpha=.33; context.fillStyle='#fff'; context.fillRect(-w*.355+lean,top+h*.455,w*.16,h*.032); context.fillRect(w*.195+lean,top+h*.455,w*.16,h*.032); context.globalAlpha=1;
    carPoly([[-w*.405-lean,top+h*.70],[w*.405-lean,top+h*.70],[w*.355-lean,top+h*.91],[-w*.355-lean,top+h*.91]],'#151515',context); const tailBase=braking?'#fff4dc':'#ff1c1c', tailGlow=braking?'#ff3b1f':'#7e0000'; if(braking){ context.save(); context.globalAlpha=.42; context.fillStyle='#ff2a00'; context.beginPath(); context.ellipse(-w*.27,top+h*.755,w*.20,h*.10,0,0,Math.PI*2); context.ellipse(w*.27,top+h*.755,w*.20,h*.10,0,0,Math.PI*2); context.fill(); context.restore(); }
    context.fillStyle=tailGlow; context.fillRect(-w*.36-lean,top+h*.705,w*.205,h*.115); context.fillRect(w*.155-lean,top+h*.705,w*.205,h*.115); context.fillStyle=tailBase; context.fillRect(-w*.325-lean,top+h*.725,w*.135,h*.065); context.fillRect(w*.190-lean,top+h*.725,w*.135,h*.065); context.fillStyle='#f1d45a'; context.fillRect(-w*.125,top+h*.795,w*.25,h*.072); context.fillStyle='#111'; context.fillRect(-w*.085,top+h*.819,w*.17,h*.014); context.fillStyle='#050505'; context.fillRect(-w*.31,top+h*.91,w*.62,h*.06); context.fillStyle='#b8b8b8'; context.fillRect(-w*.28,top+h*.955,w*.105,h*.038); context.fillRect(w*.175,top+h*.955,w*.105,h*.038); context.fillStyle='#333'; context.fillRect(-w*.255,top+h*.964,w*.055,h*.018); context.fillRect(w*.200,top+h*.964,w*.055,h*.018);
    if ((model.exhaust && accelerating) || opponentNitro || playerNitroActive) { const t=performance.now()*.012; const flame=w*(.075+Math.sin(t)*.015+speedRatio*.05); context.save(); context.globalAlpha=.88; carPoly([[-w*.235,top+h*.985],[-w*.235-flame*.42,top+h*1.105],[-w*.175,top+h*1.025]],'#ff7a00',context); carPoly([[w*.235,top+h*.985],[w*.235+flame*.42,top+h*1.105],[w*.175,top+h*1.025]],'#ff7a00',context); context.fillStyle='#ffe066'; context.fillRect(-w*.248,top+h*1.005,w*.055,h*.035); context.fillRect(w*.193,top+h*1.005,w*.055,h*.035); context.restore(); }
    carStroke([[-w*.41+lean,top+h*.31],[w*.41+lean,top+h*.31],[w*.53-lean,top+h*.66],[w*.43-lean,bottom-h*.04],[-w*.43-lean,bottom-h*.04],[-w*.53-lean,top+h*.66]],'rgba(0,0,0,.72)',Math.max(1,w*.018),context);
    if(isPlayer){ context.fillStyle='#080808'; context.fillRect(-w*.18,top+h*.525,w*.36,h*.055); context.fillStyle='#ffcc00'; context.font=`900 ${Math.max(5,Math.floor(w*.085))}px 'Courier New'`; context.textAlign='center'; context.fillText('GT',0,top+h*.568); context.textAlign='left'; if(carData&&carData.upgradeVisuals){const u=carData.upgradeVisuals;if(u.aero>=2){context.fillStyle='#111';context.fillRect(-w*.48,top+h*.08,w*.96,h*.035);}if(u.turbo>=2){context.fillStyle='#c8c8c8';context.fillRect(-w*.31,top+h*.95,w*.12,h*.05);context.fillRect(w*.19,top+h*.95,w*.12,h*.05);}if(u.tires>=3){context.fillStyle='#050505';context.fillRect(-w*.56,top+h*.47,w*.08,h*.34);context.fillRect(w*.48,top+h*.47,w*.08,h*.34);}} const dmg=Number(carData&&carData.damage||0);if(dmg>.15){context.strokeStyle='rgba(240,240,240,.65)';context.lineWidth=Math.max(1,w*.012);for(let i=0;i<Math.min(5,Math.ceil(dmg*6));i++){context.beginPath();context.moveTo(-w*.3+i*w*.12,top+h*(.55+i*.04));context.lineTo(-w*.18+i*w*.10,top+h*(.64+i*.025));context.stroke();}} }
    context.restore();
}
globalThis.drawCar = function drawCar(x,y,cw,color,isPlayer,steer,carData=null){ drawCarGeneric(ctx,x,y,cw,color,isPlayer,steer,carData,false); }

globalThis.drawSpeedLines = function drawSpeedLines() {
    if(globalThis.motionEffectsEnabled&&!motionEffectsEnabled())return;
    const force = Math.max(nitroWasActive ? 1 : 0, draftBoost * .78, perfectStartBoost > 0 ? .7 : 0);
    if (force <= .05 || speed < maxSpeed * .42) return;
    const vpX = width / 2 + renderLookaheadX * width * .06; const vpY = height * .38;
    ctx.save(); ctx.globalAlpha = .15 + force * .22; ctx.strokeStyle = '#fff'; ctx.lineWidth = qualityHigh() ? 1.25 : 1;
    const count = Math.floor((qualityHigh() ? 28 : 15) * (.55 + force * .55));
    for (let i = 0; i < count; i++) {
        const edge = rrRandom('visual') > .5 ? 1 : -1;
        const sx = edge > 0 ? width * (.58 + rrRandom('visual') * .42) : width * (.42 - rrRandom('visual') * .42);
        const sy = height * (.44 + rrRandom('visual') * .54);
        const dx = sx - vpX, dy = sy - vpY; const len = .08 + rrRandom('visual') * .12 * force;
        ctx.beginPath(); ctx.moveTo(vpX + dx * (1-len), vpY + dy * (1-len)); ctx.lineTo(sx, sy); ctx.stroke();
    }
    ctx.restore();
}

globalThis.drawRaceMessage = function drawRaceMessage() { if ((globalThis.isFtueSilent&&isFtueSilent()) || raceMessageTimer <= 0 || !raceMessage) return; const alpha = MathUtils.limit(raceMessageTimer / .35, 0, 1); ctx.save(); ctx.globalAlpha = alpha; ctx.textAlign = 'center'; const fontSize = Math.max(12, Math.floor(width * .035)); const y = Math.max(58, height * .18); ctx.font = `900 ${fontSize}px 'Courier New'`; ctx.fillStyle = 'rgba(0,0,0,.62)'; ctx.fillRect(width * .12, y - fontSize * 1.25, width * .76, fontSize * 1.85); ctx.strokeStyle = '#000'; ctx.lineWidth = Math.max(2, fontSize * .12); ctx.strokeText(raceMessage, width / 2, y); ctx.fillStyle = raceMessageColor; ctx.fillText(raceMessage, width / 2, y); ctx.restore(); }
globalThis.drawCountdown = function drawCountdown() { if (!countdownActive && goFlashTimer <= 0) return; const txt = countdownActive ? countdownText : 'GO!'; if (!txt) return; ctx.save(); const pulse = countdownActive ? 1 + Math.sin(performance.now() * .018) * .045 : 1 + goFlashTimer * .16; const fontSize = Math.max(62, Math.floor(Math.min(width, height) * (txt === 'GO!' ? .20 : .26) * pulse)); const y = Math.floor(height * .34); ctx.textAlign = 'center'; ctx.font = `900 ${fontSize}px 'Courier New'`; ctx.fillStyle = 'rgba(0,0,0,.28)'; ctx.beginPath(); ctx.arc(width / 2, y - fontSize * .22, fontSize * .70, 0, Math.PI * 2); ctx.fill(); ctx.lineWidth = Math.max(5, fontSize * .10); ctx.strokeStyle = '#000'; ctx.strokeText(txt, width / 2, y); ctx.fillStyle = txt === 'GO!' ? '#55ff55' : '#ffcc00'; ctx.fillText(txt, width / 2, y); ctx.restore(); }
